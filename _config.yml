title: Tim's Build Log
description: Things I built, learned, and figured out.
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme
defaults:
  - scope:
      path: "_posts"
    values:
      layout: post
